# -*- mode: ruby -*-
# vi: set ft=ruby :
# Embedded Vagrantfile baked into the almalinux-10 box.
Vagrant.configure("2") do |config|
  config.vm.provider "virtualbox" do |v|
    v.memory = 1024
    v.cpus   = 2
  end
end
